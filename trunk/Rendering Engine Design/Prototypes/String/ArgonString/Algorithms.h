#ifndef _ALGORITHMS_HEADER_
#define _ALGORITHMS_HEADER_

namespace Argon
{
	class Vector;
	
	//Vector SortStrings(const Vector List)
	//{
	//	Vector ReturnVector;

	//	if(List != NULL && List.size() > 0)
	//	{
	//		Vector< char* >::Iterator it;

	//		ulong LowestValue = 9999999;
	//		char* LowestChar = "";

	//		for( it = List.Begin(); it != List.End(); ++it )
	//		{
	//			const char* FirstChar = it.First();
	//			int Value = (int)FirstChar;
	//			if(Value < LowestValue)
	//			{
	//				LowestValue = Value;
	//			}
	//		}
	//	}

	//	return 0x0;
	//}

} // Namespace

#endif //_ALGORITHMS_HEADER_